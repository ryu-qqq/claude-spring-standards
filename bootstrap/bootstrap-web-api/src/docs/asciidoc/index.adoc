= API Documentation
Development Team
:doctype: book
:icons: font
:source-highlighter: highlightjs
:toc: left
:toclevels: 3
:sectlinks:
:operation-curl-request-title: Example request
:operation-http-response-title: Example response

[[overview]]
== Overview

Spring Standards REST API Documentation

[[overview-http-verbs]]
=== HTTP Methods

|===
| Method | Description

| `GET`
| Retrieve resources

| `POST`
| Create new resources

| `PUT`
| Update existing resources (full replacement)

| `PATCH`
| Partial update of resources

| `DELETE`
| Delete resources
|===

[[overview-http-status-codes]]
=== HTTP Status Codes

|===
| Status Code | Description

| `200 OK`
| Request successful

| `201 Created`
| Resource created successfully

| `204 No Content`
| Request successful, no content returned

| `400 Bad Request`
| Invalid request (validation error)

| `401 Unauthorized`
| Authentication required

| `403 Forbidden`
| Access denied

| `404 Not Found`
| Resource not found

| `409 Conflict`
| Resource state conflict

| `500 Internal Server Error`
| Server error
|===

[[overview-error-response]]
=== Error Response (RFC 7807)

All error responses follow RFC 7807 Problem Details format:

[source,json]
----
{
  "type": "about:blank",
  "title": "Bad Request",
  "status": 400,
  "detail": "Validation failed",
  "instance": "/api/v1/templates/conventions",
  "code": "VALIDATION_FAILED",
  "timestamp": "2025-01-01T00:00:00.000Z",
  "traceId": "abc123def456"
}
----

[[resources]]
== Resources

// ========================================
// Architecture API
// ========================================

[[resources-architecture]]
=== Architecture API

아키텍처를 관리하는 API입니다. 시스템의 기술 스택, 레이어 구조 등 전체 아키텍처를 정의합니다.

[[resources-architecture-create]]
==== 아키텍처 생성

`POST /api/v1/templates/architectures`

새로운 아키텍처를 생성합니다.

operation::architecture-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-architecture-update]]
==== 아키텍처 수정

`PUT /api/v1/templates/architectures/{id}`

기존 아키텍처를 수정합니다.

operation::architecture-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-architecture-search]]
==== 아키텍처 목록 조회

`GET /api/v1/templates/architectures`

아키텍처 목록을 조회합니다.

operation::architecture-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Convention API
// ========================================

[[resources-convention]]
=== Convention API

코딩 컨벤션을 관리하는 API입니다. 레이어별로 적용되는 코딩 규칙의 상위 그룹을 정의합니다.

[[resources-convention-create]]
==== 컨벤션 생성

`POST /api/v1/templates/conventions`

새로운 코딩 컨벤션을 생성합니다.

operation::convention-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-convention-update]]
==== 컨벤션 수정

`PUT /api/v1/templates/conventions/{id}`

기존 컨벤션의 이름이나 설명을 수정합니다.

operation::convention-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-convention-get-all]]
==== 컨벤션 목록 조회

`GET /api/v1/templates/conventions`

전체 컨벤션 목록을 조회합니다.

operation::convention-get-all[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Coding Rule API
// ========================================

[[resources-coding-rule]]
=== Coding Rule API

코딩 규칙을 관리하는 API입니다. 컨벤션에 속하는 개별 코딩 규칙을 정의합니다.

[[resources-coding-rule-create]]
==== 코딩 규칙 생성

`POST /api/v1/templates/coding-rules`

새로운 코딩 규칙을 생성합니다.

operation::coding-rule-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-coding-rule-update]]
==== 코딩 규칙 수정

`PUT /api/v1/templates/coding-rules/{id}`

기존 코딩 규칙을 수정합니다.

operation::coding-rule-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-coding-rule-search]]
==== 코딩 규칙 목록 조회

`GET /api/v1/templates/coding-rules`

코딩 규칙 목록을 조회합니다.

operation::coding-rule-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Zero Tolerance Rule API
// ========================================

[[resources-zero-tolerance-rule]]
=== Zero Tolerance Rule API

Zero-Tolerance 규칙을 관리하는 API입니다. PR 자동 거부 대상이 되는 필수 준수 규칙을 정의합니다.

[[resources-zero-tolerance-rule-create]]
==== Zero-Tolerance 규칙 생성

`POST /api/v1/standards/zero-tolerance-rules`

새로운 Zero-Tolerance 규칙을 생성합니다.

operation::zero-tolerance-rule-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-zero-tolerance-rule-update]]
==== Zero-Tolerance 규칙 수정

`PUT /api/v1/standards/zero-tolerance-rules/{id}`

기존 Zero-Tolerance 규칙을 수정합니다.

operation::zero-tolerance-rule-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-zero-tolerance-rule-search]]
==== Zero-Tolerance 규칙 목록 조회

`GET /api/v1/standards/zero-tolerance-rules`

전체 Zero-Tolerance 규칙 목록을 조회합니다.

operation::zero-tolerance-rule-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Rule Example API
// ========================================

[[resources-rule-example]]
=== Rule Example API

규칙 예제를 관리하는 API입니다. 코딩 규칙에 대한 GOOD/BAD 예제 코드를 정의합니다.

[[resources-rule-example-create]]
==== 규칙 예제 생성

`POST /api/v1/templates/rule-examples`

새로운 규칙 예제를 생성합니다.

operation::rule-example-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-rule-example-update]]
==== 규칙 예제 수정

`PUT /api/v1/templates/rule-examples/{id}`

기존 규칙 예제를 수정합니다.

operation::rule-example-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-rule-example-search]]
==== 규칙 예제 목록 조회

`GET /api/v1/templates/rule-examples`

규칙 예제 목록을 조회합니다.

operation::rule-example-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Checklist Item API
// ========================================

[[resources-checklist-item]]
=== Checklist Item API

체크리스트 항목을 관리하는 API입니다. 코딩 규칙 준수 여부 확인을 위한 체크리스트 항목을 정의합니다.

[[resources-checklist-item-create]]
==== 체크리스트 항목 생성

`POST /api/v1/templates/checklist-items`

새로운 체크리스트 항목을 생성합니다.

operation::checklist-item-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-checklist-item-update]]
==== 체크리스트 항목 수정

`PUT /api/v1/templates/checklist-items/{id}`

기존 체크리스트 항목을 수정합니다.

operation::checklist-item-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-checklist-item-search]]
==== 체크리스트 항목 목록 조회

`GET /api/v1/templates/checklist-items`

체크리스트 항목 목록을 조회합니다.

operation::checklist-item-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Layer API
// ========================================

[[resources-layer]]
=== Layer API

아키텍처의 레이어를 관리하는 API입니다. Domain, Application, Infrastructure 등의 레이어 정보를 정의합니다.

[[resources-layer-create]]
==== 레이어 생성

`POST /api/v1/templates/layers`

새로운 레이어를 생성합니다.

operation::layer-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-layer-update]]
==== 레이어 수정

`PUT /api/v1/templates/layers/{id}`

기존 레이어를 수정합니다.

operation::layer-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-layer-search]]
==== 레이어 목록 조회

`GET /api/v1/templates/layers`

레이어 목록을 조회합니다.

operation::layer-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Module API
// ========================================

[[resources-module]]
=== Module API

모듈을 관리하는 API입니다. 시스템의 모듈 구조와 계층 관계를 정의합니다.

[[resources-module-create]]
==== 모듈 생성

`POST /api/v1/templates/modules`

새로운 모듈을 생성합니다.

operation::module-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-module-update]]
==== 모듈 수정

`PUT /api/v1/templates/modules/{id}`

기존 모듈을 수정합니다.

operation::module-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-module-get-all]]
==== 모듈 목록 조회

`GET /api/v1/templates/modules`

전체 모듈 목록을 조회합니다.

operation::module-get-all[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Layer Dependency Rule API
// ========================================

[[resources-layer-dependency-rule]]
=== Layer Dependency Rule API

레이어 의존성 규칙을 관리하는 API입니다. 레이어 간 허용되는 의존성 방향을 정의합니다.

[[resources-layer-dependency-rule-create]]
==== 레이어 의존성 규칙 생성

`POST /api/v1/templates/layer-dependency-rules`

새로운 레이어 의존성 규칙을 생성합니다.

operation::layer-dependency-rule-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-layer-dependency-rule-update]]
==== 레이어 의존성 규칙 수정

`PUT /api/v1/templates/layer-dependency-rules/{id}`

기존 레이어 의존성 규칙을 수정합니다.

operation::layer-dependency-rule-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-layer-dependency-rule-search]]
==== 레이어 의존성 규칙 목록 조회

`GET /api/v1/templates/layer-dependency-rules`

레이어 의존성 규칙 목록을 조회합니다.

operation::layer-dependency-rule-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Package Purpose API
// ========================================

[[resources-package-purpose]]
=== Package Purpose API

패키지 목적을 관리하는 API입니다. 각 패키지의 역할과 책임을 정의합니다.

[[resources-package-purpose-create]]
==== 패키지 목적 생성

`POST /api/v1/templates/package-purposes`

새로운 패키지 목적을 생성합니다.

operation::package-purpose-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-package-purpose-update]]
==== 패키지 목적 수정

`PUT /api/v1/templates/package-purposes/{id}`

기존 패키지 목적을 수정합니다.

operation::package-purpose-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-package-purpose-search]]
==== 패키지 목적 목록 조회

`GET /api/v1/templates/package-purposes`

패키지 목적 목록을 조회합니다.

operation::package-purpose-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Package Structure API
// ========================================

[[resources-package-structure]]
=== Package Structure API

패키지 구조를 관리하는 API입니다. 모듈별 패키지 경로, 허용 클래스 타입, 네이밍 규칙 등을 정의합니다.

[[resources-package-structure-create]]
==== 패키지 구조 생성

`POST /api/v1/templates/package-structures`

새로운 패키지 구조를 생성합니다.

operation::package-structure-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-package-structure-update]]
==== 패키지 구조 수정

`PUT /api/v1/templates/package-structures/{id}`

기존 패키지 구조를 수정합니다.

operation::package-structure-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-package-structure-search]]
==== 패키지 구조 목록 조회

`GET /api/v1/templates/package-structures`

패키지 구조 목록을 조회합니다.

operation::package-structure-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Class Template API
// ========================================

[[resources-class-template]]
=== Class Template API

클래스 템플릿을 관리하는 API입니다. 클래스 타입별 기본 구조와 필수 요소를 정의합니다.

[[resources-class-template-create]]
==== 클래스 템플릿 생성

`POST /api/v1/templates/class-templates`

새로운 클래스 템플릿을 생성합니다.

operation::class-template-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-class-template-update]]
==== 클래스 템플릿 수정

`PUT /api/v1/templates/class-templates/{id}`

기존 클래스 템플릿을 수정합니다.

operation::class-template-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-class-template-search]]
==== 클래스 템플릿 목록 조회

`GET /api/v1/templates/class-templates`

클래스 템플릿 목록을 조회합니다.

operation::class-template-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// ArchUnit Test API
// ========================================

[[resources-arch-unit-test]]
=== ArchUnit Test API

ArchUnit 테스트를 관리하는 API입니다. 아키텍처 규칙 검증을 위한 테스트 코드를 정의합니다.

[[resources-arch-unit-test-create]]
==== ArchUnit 테스트 생성

`POST /api/v1/templates/arch-unit-tests`

새로운 ArchUnit 테스트를 생성합니다.

operation::arch-unit-test-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-arch-unit-test-update]]
==== ArchUnit 테스트 수정

`PUT /api/v1/templates/arch-unit-tests/{id}`

기존 ArchUnit 테스트를 수정합니다.

operation::arch-unit-test-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-arch-unit-test-search]]
==== ArchUnit 테스트 목록 조회

`GET /api/v1/templates/arch-unit-tests`

ArchUnit 테스트 목록을 조회합니다.

operation::arch-unit-test-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Resource Template API
// ========================================

[[resources-resource-template]]
=== Resource Template API

리소스 템플릿을 관리하는 API입니다. application.yml, build.gradle 등 설정 파일 템플릿을 정의합니다.

[[resources-resource-template-create]]
==== 리소스 템플릿 생성

`POST /api/v1/templates/resource-templates`

새로운 리소스 템플릿을 생성합니다.

operation::resource-template-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-resource-template-update]]
==== 리소스 템플릿 수정

`PUT /api/v1/templates/resource-templates/{id}`

기존 리소스 템플릿을 수정합니다.

operation::resource-template-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-resource-template-search]]
==== 리소스 템플릿 목록 조회

`GET /api/v1/templates/resource-templates`

리소스 템플릿 목록을 조회합니다.

operation::resource-template-search-by-cursor[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// Tech Stack API
// ========================================

[[resources-tech-stack]]
=== Tech Stack API

기술 스택을 관리하는 API입니다. Spring Boot, Java, Gradle 등 기술 스택 정보와 버전을 정의합니다.

[[resources-tech-stack-create]]
==== 기술 스택 생성

`POST /api/v1/templates/tech-stacks`

새로운 기술 스택을 생성합니다.

operation::tech-stack-create[snippets='request-fields,http-request,response-fields,http-response']

[[resources-tech-stack-update]]
==== 기술 스택 수정

`PUT /api/v1/templates/tech-stacks/{id}`

기존 기술 스택을 수정합니다.

operation::tech-stack-update[snippets='path-parameters,request-fields,http-request,response-fields,http-response']

[[resources-tech-stack-get-all]]
==== 기술 스택 목록 조회

`GET /api/v1/templates/tech-stacks`

전체 기술 스택 목록을 조회합니다.

operation::tech-stack-get-all[snippets='query-parameters,http-request,response-fields,http-response']

// ========================================
// MCP API
// ========================================

[[resources-mcp]]
=== MCP API

MCP(Model Context Protocol) 클라이언트를 위한 API입니다. AI 코드 생성 도구에서 컨벤션을 조회합니다.

[[resources-mcp-planning-context]]
==== Planning 컨텍스트 조회

`GET /api/v1/templates/mcp/planning-context`

Planning Phase를 위한 컨텍스트를 조회합니다. 레이어별 컨벤션과 규칙 정보를 반환합니다.

operation::mcp-planning-context[snippets='query-parameters,http-request,response-fields,http-response']

[[resources-mcp-module-context]]
==== Module 컨텍스트 조회

`GET /api/v1/templates/mcp/module/{moduleId}/context`

Execution Phase를 위한 Module 컨텍스트를 조회합니다. 특정 모듈의 패키지 구조와 클래스 템플릿 정보를 반환합니다.

operation::mcp-module-context[snippets='path-parameters,query-parameters,http-request,response-fields,http-response']

[[resources-mcp-validation-context]]
==== Validation 컨텍스트 조회

`GET /api/v1/templates/mcp/validation-context`

Validation Phase를 위한 컨텍스트를 조회합니다. 코딩 규칙과 체크리스트 정보를 반환합니다.

operation::mcp-validation-context[snippets='query-parameters,http-request,response-fields,http-response']

[[appendix]]
== Appendix

=== API Versioning

API version is included in the URL path:

* `/api/v1/templates/...` - Version 1 (Current)

=== Date/Time Format

All date/time values use ISO 8601 format in UTC:

* `2025-01-01T00:00:00.000Z`

=== Pagination

List endpoints support pagination with the following query parameters:

* `page` - Page number (0-based, default: 0)
* `size` - Page size (default: 20, max: 100)

Response includes pagination metadata:

[source,json]
----
{
  "content": [...],
  "page": 0,
  "size": 20,
  "totalElements": 100,
  "totalPages": 5,
  "hasNext": true,
  "hasPrevious": false
}
----
