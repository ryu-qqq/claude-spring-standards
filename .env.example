# =====================================================
# LangFuse Configuration (Optional)
# =====================================================
#
# LangFuse는 Claude Code Hook 로그를 추적하고 분석하는 플랫폼입니다.
#
# 1. 기본 동작 (이 파일 없이도 정상 작동):
#    - Hook이 실행되면 JSONL 로그가 .claude/hooks/logs/hook-execution.jsonl에 기록됩니다.
#    - LangFuse 업로드는 하지 않습니다 (빠름, 성능 우선).
#
# 2. LangFuse 사용 시 (선택적):
#    - LangFuse 계정 생성: https://cloud.langfuse.com
#    - Project 생성 후 API Key 발급
#    - 아래 환경 변수 설정
#    - 수동 업로드 스크립트 실행: python3 scripts/langfuse/upload-to-langfuse.py
#
# =====================================================

# LangFuse API Keys (필수)
# https://cloud.langfuse.com 에서 발급
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here

# LangFuse Host (기본값: US Cloud)
LANGFUSE_HOST=https://us.cloud.langfuse.com

# =====================================================
# 실시간 자동 업로드 (비권장)
# =====================================================
#
# 이 옵션을 활성화하면 Hook 실행 시마다 LangFuse에 실시간 업로드합니다.
# 성능 오버헤드: ~50-100ms (네트워크 왕복)
#
# 권장: 기본적으로 비활성화 (로그만 기록), 작업 완료 후 수동 업로드
#
# LANGFUSE_AUTO_UPLOAD=false

# =====================================================
# 사용 방법
# =====================================================
#
# 1. 이 파일을 .env로 복사:
#    cp .env.example .env
#
# 2. API Key 설정 (위의 LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY)
#
# 3. 환경 변수 로드:
#    source .env
#    또는
#    echo "source /path/to/claude-spring-standards/.env" >> ~/.zshrc
#
# 4. 작업 수행 (로그 자동 기록):
#    claude code
#
# 5. 작업 완료 후 LangFuse 업로드:
#    python3 scripts/langfuse/upload-to-langfuse.py
#
# =====================================================
